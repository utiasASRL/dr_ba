map:
  voxel_res: 1.0  # m
output:
  output_path: '/home/dl/Documents/phd/dev/dr_ba/output/ba_results'
  visualize: true
  save_result: true
input:
  data_path: '/home/dl/Documents/phd/data/boreas'
  meas_path: '/home/dl/Documents/phd/dev/dr_ba/output'
  seq_ids:
    # - 'boreas-2024-12-03-12-54'
    # - 'boreas-2025-01-08-10-59'
    # - 'boreas-2024-12-05-14-12'
    - 'boreas-2024-12-23-16-27'
  max_dist: 100.0  # m
  gauss_blur_sigma: 3.0  # pixels
  init_poses: 'dro'  # 'pogo' or 'gt', 'dro'
  init_translation_std: 0.0  # m
  init_rotation_std: 0.0  # degrees
  input_type: 'scans' # 'local_maps' or 'scans'
  local_map_res: 0.1  # m/pixel (only relevant if input_type is 'local_map')
keyframing:
  num_frames: -1
  max_kf_dist: 5.0  # m
  max_kf_rot: 30.0  # degrees
  fix_first_scan: true
optimization:
  max_iterations: 15
  convergence_tol: 1e-3
  alpha: 1.0  # step size
  adaptive_alpha: true # decrease alpha if cost does not decrease
  prior_map_std: 0.1 # intensity units
  meas_std: 0.1 # intensity units
  range_factor: 0.02  # factor to scale range uncertainty to intensity uncertainty
  use_cumul_thresh: true
  zero_thresh: 0.2 # max cumul return threshold to consider a measurement as zero return
  cumul_thresh: 0.9  # threshold to ignore measurements with too high a cumulative return
  rel_pose_prior:
    use_pose_prior: false
    translation_std: 100.0  # m
    rotation_std: 0.1  # degrees
  # Downsample control
  num_coarse_iterations: 0
  coarse_downsample: 0.2
  refine_downsample: 1.0
  tile_size: 15.0  # m, size of tiles to process separately
  max_loaded_scans: 0  # max number of scans to keep loaded in memory at once (>0 all)
mapping:
  pose_source: 'estimate' # 'estimate, 'gt', 'pogo', 'dro'
  estimate_location: '/home/dl/Documents/phd/dev/dr_ba/output/ba_results/run_98'
  frame_ranges:
    # - [0, 50]
    # - [300, 500]
    # - [3650, 3850]
    - [365, 470]
    - [3670, 3780]
localization:
  # map_location: '/home/dl/Documents/phd/dev/dr_ba/output/ba_results/map-gt-boreas-2024-12-03-12-54'
  # map_location: '/home/dl/Documents/phd/dev/dr_ba/output/ba_results/map-pogo-boreas-2024-12-03-12-54'
  map_seq: 'boreas-2024-12-05-14-12'
  # map_location: '/home/dl/Documents/phd/dev/dr_ba/output/ba_results/map-pogo-boreas-2024-12-05-14-12'
  map_location: '/home/dl/Documents/phd/dev/dr_ba/output/ba_results/ba-boreas-2024-12-05-14-12'
  start_frame: 0
  end_frame: -1  # -1 for last frame